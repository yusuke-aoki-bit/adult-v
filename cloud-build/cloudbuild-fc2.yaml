# FC2 Contents Market Crawler
# Cloud Build + Cloud Run Job設定
#
# デプロイ:
# gcloud builds submit --config=cloudbuild-fc2.yaml
#
# 手動実行:
# gcloud run jobs execute fc2-crawler --region=asia-northeast1

steps:
  # 1. Dockerイメージをビルド
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/crawler-images/fc2-crawler:latest'
      - '-f'
      - 'Dockerfile.crawler'
      - '--build-arg'
      - 'CRAWLER_SCRIPT=scripts/crawlers/crawl-fc2.ts'
      - '.'

  # 2. イメージをArtifact Registryにプッシュ
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/crawler-images/fc2-crawler:latest'

  # 3. Cloud Run Jobを作成/更新
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'jobs'
      - 'deploy'
      - 'fc2-crawler'
      - '--image'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/crawler-images/fc2-crawler:latest'
      - '--region'
      - 'asia-northeast1'
      - '--task-timeout'
      - '1800s'
      - '--max-retries'
      - '1'
      - '--memory'
      - '1Gi'
      - '--cpu'
      - '1'
      - '--set-env-vars'
      - 'DATABASE_URL=$$DATABASE_URL'
      - '--set-secrets'
      - 'DATABASE_URL=DATABASE_URL:latest'

images:
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/crawler-images/fc2-crawler:latest'

options:
  logging: CLOUD_LOGGING_ONLY

timeout: '1200s'
