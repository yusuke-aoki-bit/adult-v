# DTI Hitozuma Sites Crawler (エッチな4610, エッチな0930, 人妻斬り)
# Cloud Build + Cloud Run Job設定
#
# デプロイ:
# gcloud builds submit --config=cloudbuild-dti-hitozuma.yaml
#
# 手動実行:
# gcloud run jobs execute crawl-dti-hitozuma --region=asia-northeast1

steps:
  # 1. Dockerイメージをビルド
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/crawler/crawl-dti-hitozuma:latest'
      - '-f'
      - 'Dockerfile.crawler'
      - '--build-arg'
      - 'CRAWLER_SCRIPT=scripts/crawlers/crawl-dti-hitozuma-all.ts'
      - '.'

  # 2. イメージをArtifact Registryにプッシュ
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/crawler/crawl-dti-hitozuma:latest'

  # 3. Cloud Run Jobを作成/更新
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'gcloud'
    args:
      - 'run'
      - 'jobs'
      - 'deploy'
      - 'crawl-dti-hitozuma'
      - '--image'
      - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/crawler/crawl-dti-hitozuma:latest'
      - '--region'
      - 'asia-northeast1'
      - '--task-timeout'
      - '7200s'
      - '--max-retries'
      - '1'
      - '--memory'
      - '2Gi'
      - '--cpu'
      - '1'
      - '--set-secrets'
      - 'DATABASE_URL=database-url:latest'
      - '--args'
      - 'tsx,scripts/crawlers/crawl-dti-hitozuma-all.ts'

images:
  - 'asia-northeast1-docker.pkg.dev/$PROJECT_ID/crawler/crawl-dti-hitozuma:latest'

options:
  logging: CLOUD_LOGGING_ONLY

timeout: '1800s'
